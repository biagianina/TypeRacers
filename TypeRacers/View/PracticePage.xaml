<Page x:Class="TypeRacers.View.PracticePage"
      x:Name="pagePractice"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:vm="clr-namespace:TypeRacers.ViewModel"
      mc:Ignorable="d"
      d:DesignHeight="450"
      d:DesignWidth="800"
      Title="Practice">
    <Page.Resources>
        <vm:PracticeViewModel x:Key="ViewModel" />
    </Page.Resources>

    <Grid DataContext="{StaticResource ViewModel}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="10" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="10" />
            <RowDefinition Height="*" />
            <RowDefinition Height="20" />
            <RowDefinition Height="20" />
            <RowDefinition Height="20" />
            <RowDefinition Height="40" />
        </Grid.RowDefinitions>
        <TextBlock vm:TextBlockExtensions.BindableInlines="{Binding Inlines, UpdateSourceTrigger=PropertyChanged}"
                   FontFamily="Times New Roman"
                   Name="displayText"
                   Grid.Column="1"
                   Grid.Row="1"
                   FontSize="30"
                   TextAlignment="Center"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   TextWrapping="Wrap" />
        <Popup IsOpen="{Binding TypingAlert, UpdateSourceTrigger=PropertyChanged}"
               Width="auto"
               Height="auto"
               PlacementTarget="{Binding ElementName=displayText}"
               Placement="Top"
               AllowsTransparency="True"
               PopupAnimation="Fade">
            <Grid Width="200"
                  Height="100"
                  HorizontalAlignment="Right">
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid Background="Aquamarine">
                    <Label Content="Check your spelling!" 
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>
                </Grid>
                <Polygon Grid.Row="1"
                         Points="0,0 15,0, 0,30"
                         Stroke="Aquamarine"
                         Fill="Aquamarine" />
            </Grid>

        </Popup>
        <TextBox Name="userInput"
                 Grid.Column="1"
                 Grid.Row="3"
                 IsReadOnly="{Binding AllTextTyped}"
                 Text="{Binding CurrentInputText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 Background="{Binding InputBackgroundColor, UpdateSourceTrigger=PropertyChanged}" />

    </Grid>
</Page>

<Page x:Name="PageVersus"
      x:Class="TypeRacers.View.VersusPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:local="clr-namespace:TypeRacers.View"
      xmlns:vm="clr-namespace:TypeRacers.ViewModel"
      MinHeight="450"
      MinWidth="450"
      MaxWidth="800"
      Title="Versus"
      Style="{StaticResource AppStyle}">

    <Page.Resources>
        <vm:VersusViewModel x:Key="VersusVM" />
    </Page.Resources>

    <Grid Height="auto"
          DataContext="{StaticResource VersusVM}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0"
              VerticalAlignment="Center"
              Width="auto"
              Height="auto">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Label Content="{Binding SecondsInGame}"
                   IsEnabled="{Binding StartGameTimer}"
                   FontFamily="Times New Roman"
                   FontSize="12"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Grid.Column="1"
                   Foreground="White"
                   FontWeight="Bold"
                   Background="#FF865B8F"
                   BorderBrush="#FF5D1572">
                <Label.Effect>
                    <DropShadowEffect Color="#FF853C95" />
                </Label.Effect>
            </Label>
            <StackPanel Grid.Column="2"
                        Orientation="Vertical"
                        HorizontalAlignment="Right">
                <TextBlock FontFamily="Times New Roman"
                           FontSize="12"
                           Foreground="White"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center"
                           Text="Searching for opponents...." />
                <TextBlock FontFamily="Times New Roman"
                           FontSize="12"
                           VerticalAlignment="Top"
                           HorizontalAlignment="Center"
                           Foreground="White"
                           Text="{Binding OpponentsCount, UpdateSourceTrigger=PropertyChanged,
                            StringFormat='Number of players: {0}'}" />
            </StackPanel>
            <Button Grid.Column="0"
                    Name="Back"
                    Click="Back_click"
                    Margin="10 5"
                    Width="70"
                    Height="30"
                    HorizontalAlignment="Left"
                    Style="{StaticResource RoundButtonTemplate}"
                    Background="#FF591F74"
                    Content="Back"
                    BorderBrush="#FF9133BD"
                    BorderThickness="5"
                    FontFamily="Times New Roman"
                    FontWeight="Bold">
                <Button.Effect>
                    <DropShadowEffect Color="#FF6E1178" />
                </Button.Effect>
            </Button>
            <StackPanel Grid.Row="1"
                        Grid.ColumnSpan="3"
                        VerticalAlignment="Center">

                <local:CarAndProgressUserControl CAPUCProgress="{Binding Progress, UpdateSourceTrigger=PropertyChanged}"
                                                 CAPUCName="You"
                                                 CAPUCSliderProgress="{Binding SliderProgress, UpdateSourceTrigger=PropertyChanged}"
                                                 CAPUCSliderStyle="{StaticResource GreenSliderStyle}" />

                <local:CarAndProgressUserControl CAPUCCanBeShown="{Binding ShowFirstOpponent}"
                                                 CAPUCName="{Binding Opponents[0].Item1, UpdateSourceTrigger=PropertyChanged}"
                                                 CAPUCProgress="{Binding Opponents[0].Item2.Item1, UpdateSourceTrigger=PropertyChanged}"
                                                 CAPUCSliderProgress="{Binding Opponents[0].Item2.Item2, UpdateSourceTrigger=PropertyChanged}"
                                                 CAPUCSliderStyle="{StaticResource OrangeSliderStyle}" />

                <local:CarAndProgressUserControl CAPUCCanBeShown="{Binding ShowSecondOpponent}"
                                                 CAPUCName="{Binding Opponents[1].Item1,  UpdateSourceTrigger=PropertyChanged}"
                                                 CAPUCProgress="{Binding Opponents[1].Item2.Item1, UpdateSourceTrigger=PropertyChanged}"
                                                 CAPUCSliderProgress="{Binding Opponents[1].Item2.Item2, UpdateSourceTrigger=PropertyChanged}"
                                                 CAPUCSliderStyle="{StaticResource PinkSliderStyle}" />

            </StackPanel>
        </Grid>
        <Grid Grid.Row="1"
              VerticalAlignment="Center">
            <local:ReadAndTypeUserControl x:Name="UserCTRL"
                                          Width="auto"
                                          Height="auto"
                                          Margin="20"
                                          RATUCInlines="{Binding Inlines}"
                                          RATUCCanType="{Binding CanUserType}"
                                          RATUCGetReadyAlert="{Binding GetReadyAlert}"
                                          RATUCAllTextTyped="{Binding AllTextTyped}"
                                          RATUCCurrentInputText="{Binding CurrentInputText, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                          RATUCBackgroundColor="{Binding InputBackgroundColor, UpdateSourceTrigger=PropertyChanged}"
                                          RATUCTypingAlert="{Binding TypingAlert, UpdateSourceTrigger=PropertyChanged}"
                                          RATUCSecondsToStart="{Binding SecondsToStart, UpdateSourceTrigger=PropertyChanged}">
            </local:ReadAndTypeUserControl>
        </Grid>

        <Popup Name="GetReadyPopUp"
               IsOpen="{Binding EnableGetReadyAlert, UpdateSourceTrigger=PropertyChanged, Mode=OneWay}"
               Opened="GetReadyPopUp_Opened"
               StaysOpen="False"
               IsEnabled="{Binding EnableGetReadyAlert, UpdateSourceTrigger=PropertyChanged, Mode=OneWay}"
               Placement="Center"
               VerticalAlignment="Center"
               HorizontalAlignment="Center"
               AllowsTransparency="True">

            <Grid Width="auto"
                  Height="auto">
                <StackPanel Background="SkyBlue">
                    <Label Content="GET READY TO START!"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center" />
                    <Image Width="80"
                           Height="70"
                           Margin="2"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Source="pack://application:,,,/TypeRacers;component/Images/start.png" />
                    <Label Name="Timer"
                           Margin="5"
                           FontSize="20"
                           Content="{Binding SecondsToGetReady, UpdateSourceTrigger=PropertyChanged, Mode=OneWay}"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center" />
                </StackPanel>
            </Grid>
        </Popup>

        <Popup Name="TimerReadyPopup"
               IsOpen="{Binding EnableRestartOrExitAlert}"
               Placement="Center"
               VerticalAlignment="Center"
               HorizontalAlignment="Center"
               AllowsTransparency="True"
               Width="200"
               Height="200">

            <Border BorderThickness="4"
                    BorderBrush="#7ac5cd"
                    CornerRadius="7"
                    Padding="1"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">

                <StackPanel Background="White">
                    <TextBlock Margin="0,50,0,60"
                               FontFamily="Arial"
                               Opacity="0.9"
                               FontSize="15"
                               Foreground="#bbcedd"
                               Background="White"
                               TextAlignment="Center"
                               VerticalAlignment="Center"
                               Text="No opponents found" />

                    <StackPanel HorizontalAlignment="Center"
                                Orientation="Horizontal">
                        <Button Background="Red"
                                Foreground="White"
                                Width="100"
                                Height="70"
                                Content="TRY AGAIN"
                                Command="{Binding RestartSearchingOpponentsCommand}" />
                        <Button Background="#7ac5cd"
                                Foreground="White"
                                Width="100"
                                Height="70"
                                Content="EXIT"
                                Command="{Binding ExitProgramCommand}" />
                    </StackPanel>
                </StackPanel>
            </Border>
        </Popup>
    </Grid>

</Page>
